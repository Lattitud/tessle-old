<%= form_for(current_user.user_relationships.find_by_followed_id(@user),
             html: { method: :delete, id: "unfollow_form" },
             remote: true) do |f| %>
  <%= f.submit "Unfollow" %>
<% end %>