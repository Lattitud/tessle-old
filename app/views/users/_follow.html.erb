<%= form_for(current_user.user_relationships.build(followed_id: @user.id), remote: true, html: {id: "follow_form" }) do |f| %>
  <%= f.hidden_field :followed_id %>
  <%= f.submit "Follow" %>
<% end %>